assign(변수명으로 할문자, 넣을데이터)

ex) assign(paste0('brake0','spd',0),csvfile[csvfile[6]==0&csvfile[length(csvfile)-1]==0,length(csvfile)])

paste0의 결과 : brake0spd0

brake0spd0이란 문자열에 csvfile[csvfile[6]==0&csvfile[length(csvfile)-1]==0,length(csvfile)]의 결과를 저장.

brake0spd0 실행시 데이터가 들어간것을확인할수있음.

assign 함수는 for문을 돌리면서 변수명을 바꾸면서 데이터를 저장할 때 유용한 것 같다.
